import App from "./App";
import React from "react";
import ReactDOM from "react-dom";

// ReactDOM.render(<App />, document.getElementById("app"));

const bootstrap = () => ReactDOM.render(<App />, document.getElementById("app"));

export default bootstrap;
